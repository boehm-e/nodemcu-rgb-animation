import styled from "styled-components";

export const Card = styled.div`
    width: calc(100% - 4rem);
    margin: 2rem;
    padding: 2rem;
    box-sizing: border-box;
    background: white;
    border-radius: 1rem;
`